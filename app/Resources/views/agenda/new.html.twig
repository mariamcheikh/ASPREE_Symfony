{% extends 'EventsBundle:Default:layout.html.twig' %}

{% block content %}
    {{ form_start(form) }}
    <div class="portlet box green">
        <div class="portlet-title">
            <div class="caption">
                <i class="fa fa-gift"></i>Ajout A un agenda
            </div>
        </div>
        <div class="portlet-body form">
            <!-- BEGIN FORM-->
            <div class="form-horizontal">
                <div class="form-body">
                    <div class="form-group">
                        <label class="col-md-3 control-label">Description:</label>
                        <div class="col-md-4">
                            {{ form_widget(form.descriptionAgenda, {'label_attr': {'class': 'form-control input-circle'}}) }}

                        </div>
                    </div>

                    <div class="form-group">
                        <label class="col-md-3 control-label">Date de creation:</label>
                        <div class="col-md-4">
                            {{ form_widget(form.dateCreation, {'label_attr': {'class': 'form-control input-circle'}}) }}

                        </div>
                    </div>

                </div>
                <div class="form-actions">
                    <div class="row">
                        <div class="col-md-offset-3 col-md-9">


                            <button type="submit" id="Ajouter" class="btn red pull-right"> Ajouter </button>



                        </div>
                    </div>
                </div></div>

            <!-- END FORM-->
        </div>
    </div>
    {#<ul>#}
    {#<li>#}
    {#<a href="{{ path('agenda_index') }}"> Afficher la liste des evennement dans un agenda </a>#}
    {#</li>#}
    {#</ul>
    {{ form_widget(form, {'label_attr': {'class': 'btn btn-circle blue'}}) }}
                            {{ form_end() }}

                             <form action="#" class="form-horizontal"> </form>
    #}

    {{ form_end(form) }}
{% endblock %}
